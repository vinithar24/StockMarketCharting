import { Time } from "@angular/common";

export interface Ipo{
  id?: number,
  companyName?: string,
  stockExchange?: string,
  totalShares?: string,
  openDate?: string,
  openTime?: String,
  price?: string,
  remarks?: string
}
